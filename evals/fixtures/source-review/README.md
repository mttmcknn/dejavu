# Extracted Compose project

These are self-contained source excerpts from a project review. They are not a
runnable Gradle checkout; no wrapper, SDK or dependency cache is supplied. Use the
source and supplied evidence, and state which verification remains unrun. Do not
fetch dependencies, contact external services, or claim compilation/UI execution.
Preserve the task's write scope. Local reading and authorized source edits are
available. A release needs actual target tests in its full project separately.
